version: 0.2

phases:
  install:
    commands:
      - npm ci

  build:
    commands:
      - npm run build
      - chmod +x postScript.sh
      - cp postScript.sh dist/postScript.sh

artifacts:
  base-directory: dist
  files:
    - '**/*'
    